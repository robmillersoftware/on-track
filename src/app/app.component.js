import template from './app.html';
import controller from './app.controller';

/**
 * App component. Container that holds all
 * child components for CFI application.
 */
export const appComponent = {
  template,
  controller
};
