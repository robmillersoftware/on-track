import template from './activity.html';
import controller from './activity.controller';

/**
 * Activity component. Contains table of
 * incoming, outgoing, and balance data. Also
 * contains component for searching transactions.
 */
export const activityComponent = {
  template,
  controller
};
