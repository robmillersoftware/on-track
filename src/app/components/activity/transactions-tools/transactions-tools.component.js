import template from './transactions-tools.html';

/**
 * Transactions tools component. Contains simple
 * search type field and advanced search filters.
 */
export const transactionsToolsComponent = {
  template,
  bindings: {
    controller: '<'
  }
};
