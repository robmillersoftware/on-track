import template from './settings.html';
import controller from './settings.controller';

/**
 * Settings component. Contains the settings account selector.
 */
export const settingsComponent = {
  template,
  controller
};
