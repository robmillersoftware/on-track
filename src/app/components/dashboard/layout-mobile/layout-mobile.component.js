import template from './layout-mobile.html';
import controller from './layout-mobile.controller';

/**
 * Container component for mobile (smartphone) dashboard layout.
 */
export const layoutMobileComponent = {
  template,
  controller
};
