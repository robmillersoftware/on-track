import template from './dashboard.html';
import controller from './dashboard.controller';

/**
 * Container component for dashboard header area - mega and accounts banner -
 * that also contains dashboard cards layout area, which is can reload.
 */
export const dashboardComponent = {
  template,
  controller
};
